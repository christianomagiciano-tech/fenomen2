"""
First-party capability modules.

Empty in Phase 0, by design — see docs/architecture/overview.md
("What Phase 0 Deliberately Does Not Include"). Phase 1 adds the first
module here (`commands/`).

Every module added under this package must implement
`backend.contracts.module_base.Module` and include a `README.md`
following `docs/modules/README.md`'s template.
"""
