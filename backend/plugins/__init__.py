"""
Dynamically-discovered, third-party (or optional first-party) modules.

Empty until Phase 6, which formalizes discovery (manifest files, dynamic
loading). Until then, all modules are registered explicitly in
`backend/app.py` — see ADR 0001 and the Phase 0 summary for why dynamic
discovery is deliberately deferred rather than half-built now.
"""
